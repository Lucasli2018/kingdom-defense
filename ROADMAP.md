# kingdom-defense 升级设计规格（Design Spec）

> 本文档为「继续规划后续玩法，升级项目」的落地依据。遵循 design-first：先确认架构与功能列表，再进入编码。
> 现状：4 塔（弩箭/秘法/投石/冰霜）、4 敌+1 Boss、14 波线性关卡、塔升 3 级/可出售、离屏静态层缓存、WebAudio 合成音效、已修复攻击/受击屏幕闪烁。

---

## 0. 设计原则

- **风格一致**：延续珊瑚橙萌系、卡通画风，与领主其他项目保持视觉统一。
- **轻量架构**：保持纯前端单文件（`index.html`）+ Canvas，不引入框架；仅在 P3 阶段按需外置 `balance.json` 与 PWA 资源。
- **数据驱动**：P3 起将塔/敌人/波次数值抽到 `balance.json`，便于平衡，避免硬编码散落。
- **本地优先**：无后端，进度用 `localStorage` 存档；排行榜仅在 P2 可选接入 Gitee Pages 静态聚合。
- **性能底线**：已有离屏静态层，新增特效/实体必须复用对象池，避免每帧重分配。

---

## 1. 阶段总览

| 阶段 | 目标 | 关键交付 |
|------|------|----------|
| **P1 玩法深度** | 深化核心循环 | 3 新塔、3 新敌机制、多阶段 Boss、主动技能+能量、塔羁绊、反向路径 |
| **P2 系统广度** | 留存与进度 | 无尽模式、难度、成就、多地图、每日挑战、排行榜、存档、引导 |
| **P3 工程与技术** | 平台与体验 | 移动端、特效、BGM、平衡表、PWA、性能、i18n |

建议落地顺序：**P1 → P2 → P3**；但 P3 中的「移动端适配」与「数据驱动平衡表」可在 P1 起步时打底，降低后期返工。

---

## 2. P1 玩法深度

### 2.1 新塔

| 塔 | 定位 | 关键属性草案 | 备注 |
|----|------|--------------|------|
| **闪电塔 lightning** | 单体连锁 | cost≈110, damage≈14, range≈120, 弹射 3–4 目标, 每跳衰减 25%, 可对空 | 命中时电弧连线可视化 |
| **火焰塔 flame** | 范围灼烧 DoT | cost≈130, dps≈10, duration≈2.5s, 不可对空, 短射程 | 持续伤害，不触发暴击 |
| **增益塔 aura** | 辅助光环 | cost≈90, 自身不攻击, 范围≈110, 提升友塔 攻速 +15% / 伤害 +12% | 可叠加（设上限） |

### 2.2 新敌人机制

- **分裂怪 splitter**：死亡时分裂为 2 只小怪（hp 减半、速度略快），计入击杀奖励。
- **隐身怪 stealth**：常态不可见且不可被锁定，需「反隐」来源（闪电塔自带反隐 / 或升级项）才能攻击；用半透明轮廓提示。
- **治疗怪 healer**：每 1s 为半径内友军回复少量 hp；优先集火目标。

### 2.3 多阶段 Boss

- 基础沿用巨魔×6，新增阶段阈值：**66% / 33%** 触发：
  - 阶段二：召唤 2 只小怪 + 自身移速 +20%。
  - 阶段三（狂暴）：伤害 ×1.5、红光环增强（复用现有 boss 红光但去掉脉动，保持稳态）。
- Boss 出场 toast 已存在，补充阶段切换提示与音效。

### 2.4 主动技能 + 能量系统

- **能量条**：随时间缓慢回复（每秒 +X），击杀额外 +Y。
- 三个技能（按钮 + 快捷键 Q/W/E）：
  1. **全屏冰冻**：敌人暂停 3s（保留塔攻击）。
  2. **陨石**：指定/随机落点范围高伤 + 短暂灼烧。
  3. **战意**：所有塔攻速 ×2 持续 5s。
- UI：底部技能栏 + 冷却遮罩，能量不足时禁用。

### 2.5 塔羁绊

- 相邻特定组合触发被动：如「冰霜 + 秘法」= 溅射范围内附带减速；「增益塔 + 任意」= 增益范围 +20%。
- 实现：建塔时扫描邻域计算羁绊标记，绘制连接线提示。

### 2.6 反向路径阻挡

- 可放置一次性「路障」迫使敌人绕行（路径重算）；或限时阻挡。需重算 `posOnPath` 与航点。
- 优先级低，作为 P1 末期可选 feature。

---

## 3. P2 系统广度

- **无尽模式**：第 15 波后进入无限生成，敌人属性随波数线性增长，记录最高波数（`localStorage`）。
- **难度选择**：简单/普通/困难，倍率 hp×[0.8/1/1.3]、reward×[1.2/1/0.85]。
- **成就系统**：首杀 Boss、无伤通关、单局 50 击杀、全塔满级等，弹窗 + 本地记录。
- **多地图/关卡**：`PATH` 与装饰模块化，提供 2–3 张地图（草地/雪原/荒漠），切换即换 `balance` 与素材。
- **每日挑战**：基于日期种子的固定波次，公平排行。
- **排行榜**：本地榜 + 可选导出到 Gitee Pages 静态 JSON（只读聚合）。
- **存档**：`localStorage` 保存金币/最高波数/成就/已解锁地图；开始界面「继续/新游戏」。
- **新手引导**：首次进入分步提示（选塔→建造→开始波次→技能）。

---

## 4. P3 工程与技术

- **移动端触屏适配**：`canvas` 等比缩放 + 触摸事件映射；底部虚拟塔栏/技能栏；防止误触与双击缩放。
- **视觉特效升级**：塔攻击动作帧、敌人受击抖动/血花、技能特效粒子（对象池复用）。
- **BGM + 音效库**：循环背景乐（低音量）+ 更丰富命中/技能音；设置页音量开关。
- **数据驱动平衡表**：`balance.json`（`TOWERS`/`ENEMIES`/`WAVES`/难度倍率），启动 `fetch` 加载，便于调参。
- **PWA 离线**：`manifest.webmanifest` + `sw.js` 缓存，支持「添加到主屏幕」、离线可玩。
- **性能**：实体对象池、分层离屏（塔层/敌人层/特效层）、`dt` 上限已具备。
- **i18n**：文案抽 `i18n.json`（中/英），设置切换。

---

## 5. 实施顺序与里程碑

- **M1（P1 基础，优先）**：闪电塔 + 能量/主动技能 + 多阶段 Boss → 可玩验证。
- **M2（P1 扩展）**：火焰塔 + 增益塔 + 分裂/隐身/治疗怪 + 塔羁绊（+ 反向路径可选）。
- **M3（P2）**：无尽模式 + 难度 + 存档 + 成就 + 多地图。
- **M4（P3）**：移动端 + 特效 + BGM + 平衡表 + PWA。

> 建议把 P3 的「移动端适配」与「数据驱动平衡表」前置到 M1/M2 打底。

---

## 6. 风险与取舍

- **单文件膨胀**：长期看 `index.html` 会变大；P3 用 `balance.json` 外置缓解，代码仍单文件部署。
- **平衡复杂度**：数值先用草案，依赖 `balance.json` 快速迭代。
- **移动端性能**：新增实体必须走对象池，避免低端机掉帧（掉帧本身也会表现为「闪」）。

---

## 7. 下一步

确认本规格后，从 **M1** 起步：先细化「闪电塔 + 能量/主动技能 + 多阶段 Boss」三项的确切数据结构与交互，再编码实现并本地验证。
